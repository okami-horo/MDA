# 洗词条任务说明

从人物“详情资讯”页开始任务。先选择重洗内容，再选择角色或单件范围。

| 重洗内容   | 洗角色                                                             | 洗单件装备                                     |
| ---------- | ------------------------------------------------------------------ | ---------------------------------------------- |
| 洗词条效果 | 四件装备共同满足各效果的条数配额，可设置禁止、不要求或需求 1～4 条 | 只处理选定部位，配置最多三项需求，并可限定槽位 |
| 洗词条数值 | 四件装备中同名效果的 T 档分别累加，按总 T 达标                     | 按选定装备中对应效果的 T 档达标                |

## 数值目标

角色数值目标为 1～60，单件为 1～15；0 表示忽略，至少设置一项。默认目标分别为优越代码总 T44、单件 T11。T 档表示词条数值档位，不是装备强化等级。

角色目标不要求四件均匀达标。例如总 T40 可以由 T15、T10、T10、T5 组成。只有 n 条同名效果时，最高为 15×n；缺失效果贡献 0。数值重洗不会生成新的效果，目标超过实际可达上限时停止。

数值模式按完成全部目标的预计材料成本取舍结果，可能接受某项数值下降、目标缺口扩大，以换取整体预计成本降低；不保证每项数值只升不降，也不保证全局最少耗材。

## 材料与停止条件

任务使用现有订制模组和自订密钥，不购买材料。数值模式必要时会免费解除锁；已消耗的锁定材料不返还。选择锁定材料和加载先前锁定设置本身不扣库存，实际费用在确认页读取、结果出现后记账。

历史锁恢复只复用本任务最近实际使用且符合当前完整计划的设置，恢复后仍需核验。余额不足、页面未知、识别数据不完整或锁状态无法验证时，不应继续盲目重洗。

四种组合都会在初始扫描完成后、重洗前显示预计需要的订制模组数。估算不使用密钥，计入模型中的模组加锁成本并向上取整；已达标显示 0，无法估算时给出提示。它不是库存要求或保底上限，数值模式可能偏高。独立调试扫描不输出估算。

本任务属于高级任务；没有可用专项额度时，按 5 倍额度消耗。当前验证范围及尚待验收的内容统一见[验证边界](洗词条数值实现.md#验证边界)。

## 开发文档索引

| 文档                                              | 内容                                                     |
| ------------------------------------------------- | -------------------------------------------------------- |
| [洗词条数值实现](洗词条数值实现.md)               | 数值分支配置接线、Pipeline／Go 分工、执行约束与验证边界  |
| [洗词条策略与Agent逻辑](洗词条策略与Agent逻辑.md) | 效果分支的角色／单件策略、锁定决策与实现参考             |
| [洗词条概率与期望计算](洗词条概率与期望计算.md)   | 两类重洗的概率模型、数值成本估值、候选接受准则及历史基线 |
| [装备系统与洗词条研究](装备系统与洗词条研究.md)   | 游戏内数值、UI 文案和材料规则依据                        |
| [装备改造系统调整报告](装备改造系统调整报告.md)   | 装备改造规则调整与公告文字存档                           |

运行时代码位于 `agent/go-service/equipmentreroll/`，识别与操作节点位于 `assets/resource/pipeline/EquipmentReroll/`，选项位于 `assets/tasks/EquipmentReroll.json`。配置分别通过 `attach.operation` 与 `attach.mode` 表达重洗内容和范围，不能仅凭范围判断是哪类任务。

个人日志、原始截图、一次性评估和排障记录不作为公开使用说明。公开文档保留可复用结论，技术行为以当前源码与正式测试为准。
